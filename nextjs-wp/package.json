{
  "name": "@codecabana/nextjs-wp",
  "version": "0.0.1",
  "description": "Starter template for a headless Wordpress + NextJS frontend",
  "scripts": {
    "dev": "run-s wp:start next:dev",
    "wp:start": "npm run start --prefix wp",
    "wp:bootstrap": "npm run bootstrap --prefix wp",
    "next:dev": "npm run dev --prefix nextjs"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
